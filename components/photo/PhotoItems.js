import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const PhotoItems = () => {
  return (
    <View>
      <Text>PhotoItems</Text>
    </View>
  )
}

export default PhotoItems

const styles = StyleSheet.create({})