import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const CreditItems = () => {
  return (
    <View>
      <Text>CreditItems</Text>
    </View>
  )
}

export default CreditItems

const styles = StyleSheet.create({})